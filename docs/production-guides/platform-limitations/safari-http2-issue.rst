ناسازگاری Safari با HTTP/2
==========================
محدودیت:
Safari ممکن است در برخی سناریوها با HTTP/2 اتصال را قطع کند.
راهکار:
غیرفعال کردن HTTP/2 در Ingress کلاستر (در صورت وجود کاربران Safari به‌صورت قابل توجه).

